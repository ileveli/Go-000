sql.ErrNoRows 表示没有符合条件的数据，不同的业务场景下，表达的业务含义不一样，比如：
  1、对于登录接口，没有查询到用户信息，业务含义表示：不存在用户；
  2、查询订单接口，没有查询到订单信息，业务含义表示：订单信息为空；
所以DAO层调用触发sql.ErrNoRows，无需处理，直接抛给service层即可；

service层根据不同的业务场景，wrap sql.ErrNoRows 增加业务msg信息 抛给API层；
API层根据业务wrap msg信息，做相应的业务处理，比如对于登录接口调用，提示调用方用户信息不存在；
对于订单查询接口，提示无订单信息；
